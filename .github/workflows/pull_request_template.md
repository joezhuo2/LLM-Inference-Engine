## What changed

## How it was tested
- [ ] `ctest --preset debug` passes on the GPU (laptop or over SSH)
- [ ] `compute-sanitizer` clean for new kernels

## Performance (if relevant)
| Metric | Before | After |
| --- | --- | --- |

## Screenshots (ncu / nsys, if relevant)
